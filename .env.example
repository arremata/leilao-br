# Local UI and backend connected to the production database.
# This credential may read and write production data.
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/postgres

# Optional when the project virtualenv is elsewhere.
# ARREMATE_PYTHON=/absolute/path/to/python
